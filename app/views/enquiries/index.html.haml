%h1 Listing enquiries

%table
  %thead
    %tr
      %th Name
      %th Email
      %th Body
      %th
      %th
      %th

  %tbody
    - @enquiries.each do |enquiry|
      %tr
        %td= enquiry.name
        %td= enquiry.email
        %td= enquiry.body
        %td= link_to 'Show', enquiry
        %td= link_to 'Edit', edit_enquiry_path(enquiry)
        %td= link_to 'Destroy', enquiry, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Enquiry', new_enquiry_path
