- if product.product_images.any?
  = link_to(image_tag(product.product_images.first.image.thumbnail), product)
%h3= link_to product.name, product
.price
  = number_to_currency product.price, locale: :en
= button_to "Add to Cart", add_to_cart_path(product_id: product)