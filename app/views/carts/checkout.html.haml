#form_container.checkout_form
  %h1= "Checkout - #{@cart.checkout_status.humanize}"
  - if @cart.errors.any?
    #error_container
      - @cart.errors.messages.each do |error|
        .error= error
  = render "carts/checkout_forms/#{@cart.checkout_status}"