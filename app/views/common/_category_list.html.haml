- if shop_site?
  %h2.side_menu_heading= link_to 'Categories', categories_path
  %ul.side_menu_list
    - @site.categories.each do |category|
      %li= link_to category.name, category
- else

  %ul.side_menu_list
    - @site.pages.side_pages.each do |page|
      %li= link_to page.name, page